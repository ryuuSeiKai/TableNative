---
title: PostgreSQL
description: Kết nối PostgreSQL 12+ với duyệt schema, hiển thị JSONB và driver libpq đầy đủ
---

# Kết nối PostgreSQL

TablePro hỗ trợ PostgreSQL 12 trở lên qua libpq C connector. Duyệt schema, hiển thị kiểu dữ liệu phong phú (JSONB, arrays, UUID, inet) và mọi tính năng truy vấn PostgreSQL chuẩn đều hoạt động ngay.

## Thiết lập nhanh

<Steps>
  <Step title="Mở Form kết nối">
    Nhấp **New Connection** từ màn hình Chào mừng hoặc **File** > **New Connection**
  </Step>
  <Step title="Chọn PostgreSQL">
    Chọn **PostgreSQL** từ danh sách loại database
  </Step>
  <Step title="Nhập thông tin kết nối">
    Điền host, port, username, password và tên database
  </Step>
  <Step title="Kiểm tra và kết nối">
    Nhấp **Test Connection**, rồi **Create**
  </Step>
</Steps>

## Cài đặt kết nối

### Trường bắt buộc

| Trường | Mô tả | Mặc định |
|-------|-------------|---------|
| **Name** | Tên định danh kết nối | - |
| **Host** | Hostname hoặc IP server | `localhost` |
| **Port** | Cổng PostgreSQL | `5432` |
| **Username** | Người dùng PostgreSQL | `postgres` |
| **Password** | Mật khẩu | - |
| **Database** | Tên database | - |

<Warning>
Khác MySQL, PostgreSQL bắt buộc chỉ định tên database khi kết nối.
</Warning>

<Tip>
Bạn cũng có thể kết nối bằng URL `postgresql://` từ trình duyệt hoặc terminal. Xem [Tham chiếu URL kết nối](/databases/connection-urls).
</Tip>

{/* Screenshot: Biểu mẫu kết nối PostgreSQL */}
<Frame caption="Form kết nối PostgreSQL">
  <img
    className="block dark:hidden"
    src="/images/postgresql-connection-form.png"
    alt="Form kết nối PostgreSQL"
  />
  <img
    className="hidden dark:block"
    src="/images/postgresql-connection-form-dark.png"
    alt="Form kết nối PostgreSQL"
  />
</Frame>

## Ví dụ cấu hình

### Server dev local

```
Name:     Local PostgreSQL
Host:     localhost
Port:     5432
Username: postgres
Password: (mật khẩu)
Database: postgres
```

### Docker PostgreSQL Container

```
Name:     Docker Postgres
Host:     localhost
Port:     5432 (hoặc cổng mapped)
Username: postgres
Password: (mật khẩu từ POSTGRES_PASSWORD)
Database: postgres
```

### Homebrew PostgreSQL

```
Name:     Homebrew Postgres
Host:     localhost
Port:     5432
Username: (tên user macOS)
Password: (để trống cho local trust auth)
Database: postgres
```

<Tip>
Homebrew PostgreSQL thường dùng xác thực "trust" cho kết nối local, không cần mật khẩu.
</Tip>

### Server từ xa

```
Name:     Production DB
Host:     db.example.com
Port:     5432
Username: app_user
Password: (mật khẩu an toàn)
Database: myapp_production
```

### Cloud Databases

#### Amazon RDS

```
Name:     AWS RDS Postgres
Host:     mydb.abc123.us-east-1.rds.amazonaws.com
Port:     5432
Username: postgres
Password: (mật khẩu RDS)
Database: postgres
```

#### Heroku Postgres

```
Name:     Heroku Postgres
Host:     ec2-xx-xxx-xxx-xxx.compute-1.amazonaws.com
Port:     5432
Username: (từ DATABASE_URL)
Password: (từ DATABASE_URL)
Database: (từ DATABASE_URL)
```

<Note>
Phân tích `DATABASE_URL` của Heroku để lấy host, port, username, password và tên database.
</Note>

## Tính năng

### Hỗ trợ Schema

PostgreSQL tổ chức bảng thành schemas. TablePro hiển thị:

1. Tất cả schemas user có quyền truy cập
2. Các bảng trong mỗi schema
3. Thứ tự search path

{/* Screenshot: Sidebar showing PostgreSQL schemas and tables */}
<Frame caption="Trình duyệt schema PostgreSQL">
  <img
    className="block dark:hidden"
    src="/images/postgresql-schema-browser.png"
    alt="Schema browser"
  />
  <img
    className="hidden dark:block"
    src="/images/postgresql-schema-browser-dark.png"
    alt="Schema browser"
  />
</Frame>

### Chuyển đổi Database

Chuyển giữa các database và schema bằng switcher (**Cmd+K**):

1. Nhấn **Cmd+K** để mở switcher. Hiển thị danh sách database cho kết nối PostgreSQL
2. Chọn database đích (ví dụ: `app`, `analytics`, `postgres`)
3. Sidebar, truy vấn và toolbar cập nhật sau khi kết nối lại

<Tip>
Database hệ thống (`postgres`, `template0`, `template1`) được đánh dấu là hệ thống.
</Tip>

<Note>
Chuyển database sẽ kết nối lại phiên PostgreSQL đến database đã chọn.
</Note>

### Thông tin bảng

Mỗi bảng hiển thị:

- **Structure**: Cột với kiểu dữ liệu PostgreSQL
- **Indexes**: Bao gồm partial và expression indexes
- **Constraints**: Primary keys, foreign keys, check constraints
- **DDL**: Câu lệnh CREATE TABLE

### Thực thi truy vấn

Hỗ trợ đầy đủ cú pháp PostgreSQL:

```sql
-- Truy vấn JSONB
SELECT data->>'name' as name, data->'address'->>'city' as city
FROM customers
WHERE data @> '{"active": true}'::jsonb;

-- Thao tác Array
SELECT name, tags
FROM products
WHERE 'electronics' = ANY(tags);

-- Window functions
SELECT
    department,
    employee,
    salary,
    salary - LAG(salary) OVER (PARTITION BY department ORDER BY hire_date) as salary_change
FROM employees;

-- CTEs với RETURNING
WITH inserted AS (
    INSERT INTO orders (customer_id, total)
    VALUES (1, 99.99)
    RETURNING *
)
SELECT * FROM inserted;

-- Full-text search
SELECT title, ts_rank(search_vector, query) as rank
FROM articles, to_tsquery('english', 'database & performance') query
WHERE search_vector @@ query
ORDER BY rank DESC;
```

### Kiểu dữ liệu PostgreSQL

TablePro hiển thị đúng hệ thống kiểu phong phú của PostgreSQL:

| Kiểu | Hiển thị |
|------|---------|
| `jsonb` | JSON format |
| `array` | Ký hiệu array |
| `uuid` | Chuỗi UUID |
| `inet` | Địa chỉ IP |
| `timestamp with time zone` | Timestamp bản địa hóa |
| `interval` | Khoảng thời gian dễ đọc |
| `bytea` | Chỉ thị dữ liệu binary |

## Khắc phục sự cố

### Connection Refused

**Triệu chứng**: "Connection refused" hoặc "could not connect to server"

**Nguyên nhân và Giải pháp**:

1. **PostgreSQL không chạy**
   ```bash
   # Kiểm tra PostgreSQL
   pg_isready -h localhost -p 5432

   # Khởi động (macOS Homebrew)
   brew services start postgresql@16
   ```

2. **PostgreSQL không chấp nhận kết nối**
   - Kiểm tra `listen_addresses` trong `postgresql.conf`
   - Cho kết nối remote: `listen_addresses = '*'`

3. **Firewall chặn kết nối**
   - Kiểm tra firewall macOS
   - Xác minh cổng 5432 truy cập được

### Authentication Failed

**Triệu chứng**: "password authentication failed" hoặc "Peer authentication failed"

**Về pg_hba.conf**:

PostgreSQL dùng `pg_hba.conf` để kiểm soát xác thực:

```
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
```

**Giải pháp**:

1. **Cho password auth**: Đảm bảo `md5` hoặc `scram-sha-256` được đặt
2. **Cho dev local**: Dùng `trust` (không cần mật khẩu)
3. **Reset password**:
   ```sql
   ALTER USER postgres WITH PASSWORD 'newpassword';
   ```

### Database Does Not Exist

**Triệu chứng**: "database 'xyz' does not exist"

**Giải pháp**: Kết nối database có sẵn trước (thường là `postgres`), rồi tạo database:

```sql
CREATE DATABASE myapp;
```

### Permission Denied

**Triệu chứng**: "permission denied for table" hoặc "permission denied for schema"

**Giải pháp**:

1. Kiểm tra quyền hiện tại:
   ```sql
   SELECT * FROM information_schema.role_table_grants
   WHERE grantee = 'username';
   ```

2. Cấp quyền:
   ```sql
   GRANT ALL ON DATABASE mydb TO username;
   GRANT ALL ON SCHEMA public TO username;
   GRANT ALL ON ALL TABLES IN SCHEMA public TO username;
   ```

### Kết nối SSL/TLS

Cấu hình SSL trong phần **SSL/TLS** của form kết nối.

| Chế độ SSL | PostgreSQL `sslmode` | Mô tả |
|----------|---------------------|-------------|
| **Disabled** | `disable` | Không mã hóa SSL |
| **Preferred** | `prefer` | Dùng SSL nếu có, không thì kết nối thường |
| **Required** | `require` | Bắt buộc SSL, không xác minh chứng chỉ |
| **Verify CA** | `verify-ca` | Bắt buộc SSL và xác minh chứng chỉ server với CA |
| **Verify Identity** | `verify-full` | Bắt buộc SSL, xác minh CA và hostname server |

Với **Verify CA** và **Verify Identity**, cung cấp đường dẫn chứng chỉ CA (root certificate). Tùy chọn thêm file chứng chỉ client và key cho mutual TLS.

<Tip>
Cloud providers như AWS RDS, Heroku, Supabase thường yêu cầu SSL. Dùng **Required** hoặc **Verify CA** tùy nhu cầu bảo mật.
</Tip>

<Note>
Nếu không muốn cấu hình chứng chỉ SSL, [SSH tunneling](/vi/databases/ssh-tunneling) mã hóa toàn bộ traffic qua tunnel SSH.
</Note>

{/* Screenshot: Cấu hình SSL/TLS PostgreSQL */}
<Frame caption="Cấu hình SSL/TLS PostgreSQL">
  <img
    className="block dark:hidden"
    src="/images/postgresql-ssl-config.png"
    alt="Cấu hình SSL/TLS PostgreSQL"
  />
  <img
    className="hidden dark:block"
    src="/images/postgresql-ssl-config-dark.png"
    alt="Cấu hình SSL/TLS PostgreSQL"
  />
</Frame>

## PostgreSQL Extensions

TablePro hoạt động với database dùng các extension phổ biến:

| Extension | Hỗ trợ |
|-----------|---------|
| `postgis` | Hiển thị kiểu geometry/geography |
| `pg_trgm` | Hỗ trợ truy vấn đầy đủ |
| `uuid-ossp` | Hiển thị UUID |
| `hstore` | Hiển thị key-value |
| `ltree` | Hiển thị tree path |

Xem extension đã cài:

```sql
SELECT * FROM pg_extension;
```

## Mẹo hiệu năng

### Phân tích truy vấn

Dùng `EXPLAIN ANALYZE` để xem hiệu năng truy vấn:

```sql
EXPLAIN ANALYZE
SELECT * FROM users WHERE email LIKE '%@example.com';
```

### Giới hạn kết nối

PostgreSQL có giới hạn kết nối mặc định. Giám sát:

```sql
SELECT count(*) FROM pg_stat_activity;
```

### Bảng lớn

Cho bảng hàng triệu dòng:

1. Luôn dùng `LIMIT` trong truy vấn khám phá
2. Tạo index phù hợp
3. Dùng phân trang trong data grid

## Khác biệt với MySQL

Nếu bạn từ MySQL:

| MySQL | PostgreSQL |
|-------|------------|
| `SHOW DATABASES` | `\l` hoặc query `pg_database` |
| `SHOW TABLES` | `\dt` hoặc query `information_schema.tables` |
| `DESCRIBE table` | `\d table` hoặc query `information_schema.columns` |
| `AUTO_INCREMENT` | `SERIAL` hoặc `GENERATED AS IDENTITY` |
| `LIMIT x, y` | `LIMIT y OFFSET x` |
| Backtick quotes | Double quotes cho identifiers |

## Bước tiếp theo

<CardGroup cols={2}>
  <Card title="SSH Tunnel" icon="lock" href="/vi/databases/ssh-tunneling">
    Kết nối an toàn đến PostgreSQL server từ xa
  </Card>
  <Card title="Trình soạn thảo SQL" icon="code" href="/vi/features/sql-editor">
    Các tính năng SQL editor
  </Card>
  <Card title="Nhập/Xuất" icon="file-import" href="/vi/features/import-export">
    Import và export dữ liệu PostgreSQL
  </Card>
  <Card title="Cấu trúc Bảng" icon="table" href="/vi/features/table-structure">
    Xem và sửa cấu trúc bảng
  </Card>
</CardGroup>
